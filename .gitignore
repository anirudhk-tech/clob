# ---------- Build output (CMake / general) ----------
/build*/                 # Out-of-source CMake build folders like build/, build-debug/, build-release/
build-asan/
build-rel/
/cmake-build-*/          # Common CLion-style build folders
/out/                    # Some folks use out/ for build artifacts
build
.cache

CMakeFiles/              # CMake-generated directory (in-source builds)
CMakeCache.txt           # CMake configure cache (generated)
cmake_install.cmake      # Generated install script
CTestTestfile.cmake      # Generated CTest file
Makefile                 # Generated by some generators (Unix Makefiles)
install_manifest.txt     # Generated install manifest

compile_commands.json    # Generated compilation database (often symlinked/copied for tooling)

# ---------- Tooling / IDE ----------------------
.cache/                  # clangd background index cache
.vscode/                 # VS Code settings
.idea/                   # JetBrains IDE settings
*.user                   # Qt Creator / some IDE user files

# ---------- OS junk ----------------------------
.DS_Store                # macOS Finder metadata
Thumbs.db                # Windows Explorer metadata

# ---------- Compiled artifacts ----------
*.o                      # Object files
*.obj                    # Object files (MSVC)
*.a                      # Static libraries (Unix)
*.lib                    # Static libraries (MSVC)
*.so                     # Shared libraries (Linux)
*.dylib                  # Shared libraries (macOS)
*.dll                    # Shared libraries (Windows)
*.exe                    # Executables (Windows)
*.out                    # Executables (common on Unix)
*.pdb                    # Debug symbols (MSVC)
*.dSYM/                  # Debug symbols bundle (macOS)

# ---------- Sanitizers / profilers ----------
*.profraw                # LLVM profile raw output
*.profdata               # LLVM profile merged data

# ---------- Logs ----------------------------
*.log                    # Log files

